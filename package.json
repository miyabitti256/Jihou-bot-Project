{
  "name": "jihou-bot-project",
  "devDependencies": {
    "@biomejs/biome": "^2.4.11",
    "husky": "^9.1.7",
    "ts-analyzer": "^1.3.7"
  },
  "overrides": {
    "hono": "^4.12.12",
    "zod": "^4.3.6"
  },
  "private": true,
  "scripts": {
    "dev": "bun run --filter discord-bot dev",
    "dev:front": "bun run --filter front-app dev",
    "build": "bun run --filter front-app build",
    "typecheck": "tsc --noEmit -p discord-bot/tsconfig.json && tsc --noEmit -p front-app/tsconfig.json",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format --write .",
    "prepare": "husky",
    "deploy:bot": "fly deploy --config discord-bot/fly.toml"
  },
  "workspaces": [
    "discord-bot",
    "front-app",
    "packages/*"
  ],
  "dependencies": {
    "biome": "^0.3.3"
  }
}
